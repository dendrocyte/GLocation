<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity android:name="com.dendrocyte.haylocation.demo.MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout">
            <!--not to handle recreate activity due to screen orientation change-->


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.dendrocyte.haylocation.demo.UtilViewActivity"
            android:exported="false"
            />
        <activity android:name="com.dendrocyte.haylocation.demo.UtilFunctionActivity"
            android:exported="false"
            />
        <activity android:name="com.dendrocyte.haylocation.demo.UtilWorkerActivity"
            android:exported="false"
            />

    </application>

</manifest>